name: officer_interface
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🔐 Secure storage
  flutter_secure_storage: ^9.0.0

  # 🌐 HTTP requests
  http: ^1.0.0

  # 🔤 Fonts
  google_fonts: ^6.1.0

  # 📸 Camera / Gallery picker (Plate OCR 必需)
  image_picker: ^1.0.7

  # 🍎 iOS-style icons
  cupertino_icons: ^1.0.8

  # 🌍 Date / currency formatting
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/icon/

# ===============================
# App Launcher Icon Configuration
# ===============================
flutter_launcher_icons:
  android: true
  ios: true
  min_sdk_android: 21

  # Android & iOS app icon
  image_path_android: assets/icon/app_icon_bg.png
  image_path_ios: assets/icon/app_icon_bg.png

  # Android adaptive icon
  adaptive_icon_background: assets/icon/app_icon_bg.png
  adaptive_icon_foreground: assets/icon/app_icon_fg.png
